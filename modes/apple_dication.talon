mode: command
-

# Apple dication mode
apple mode:
    speech.disable()
    # should match the apple dictation setting 
    key(ctrl-cmd-shift-alt-f10)


# todo:
# make this a mode
# then make `apple mode off` a command in that mode that
# presses any key (to turn off dication) then renables the talon 
    