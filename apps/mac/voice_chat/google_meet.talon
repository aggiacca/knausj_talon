os: mac
app: Google Chrome
title: /Meet -/
-
tag(): voice_chat

action(user.toggle_mute):
	key(cmd-d)

action(user.toggle_video):
	key(cmd-e)

# No short cut I believe
#action(user.toggle_screen_share):
#	key(shift-cmd-s)

